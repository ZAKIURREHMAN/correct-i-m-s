{
  "name": "inventory-management-system-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "electron:dev": "ELECTRON_DEV=1 electron electron/main.js",
    "electron:prod": "npm --prefix client/my-app run build && electron electron/main.js",
    "client:dev": "npm --prefix client/my-app run dev",
    "server:dev": "npm --prefix server run dev || node server/index.js"
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}